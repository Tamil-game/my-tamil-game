import streamlit as st
import random
import time
# பக்கத்தின் அமைப்பு
st.set_page_config(page_title="மெகா கல்வி விளையாட்டு", page_icon="🎮", layout="wide")

# பின்னணி மற்றும் எழுத்து வடிவம் (CSS)
st.markdown("""
    <style>
    .stApp {
        background: linear-gradient(to right, #1e3c72, #2a5298);
        color: white;
    }
    .game-card {
        background-color: rgba(255, 255, 255, 0.1);
        padding: 20px;
        border-radius: 15px;
        border: 2px solid #ffffff;
        margin-bottom: 25px;
    }
    h1, h2, h3, p {
        color: white !important;
    }
    </style>
    """, unsafe_allow_html=True)

st.title("🎓 6 கேள்விகள் - 2 வெவ்வேறு விளையாட்டுகள்!")

# --- விளையாட்டு 1: ரேடியோ ஸ்டைல் ---
st.header("🕹️ விளையாட்டு 1: கணிதம் & பொது அறிவு")
st.markdown("<div class='game-card'>", unsafe_allow_html=True)

c1, c2, c3 = st.columns(3)
with c1:
    ans1 = st.radio("1. 15 + 25 எவ்வளவு?", ["--", "30", "40", "50", "35"], key="g1")
with c2:
    ans2 = st.radio("2. கணினியின் 'மூளை' எது?", ["--", "Keyboard", "CPU", "Monitor", "Mouse"], key="g2")
with c3:
    ans3 = st.radio("3. வாரத்தின் முதல் நாள் எது?", ["--", "திங்கள்", "ஞாயிறு", "புதன்", "சனி"], key="g3")

if st.button("விளையாட்டு 1 சரிபார்"):
    s1 = 0
    if ans1 == "40": s1 += 1
    if ans2 == "CPU": s1 += 1
    if ans3 == "ஞாயிறு": s1 += 1
    st.success(f"விளையாட்டு 1 மதிப்பெண்: {s1}/3")
    if s1 == 3: st.balloons()
st.markdown("</div>", unsafe_allow_html=True)

# --- விளையாட்டு 2: செலக்ட் பாக்ஸ் ஸ்டைல் ---
st.header("🚀 விளையாட்டு 2: அறிவியல் & இயற்கை")
st.markdown("<div class='game-card'>", unsafe_allow_html=True)

q4 = st.selectbox("4. இலங்கையின் தேசிய மலர் எது?", ["--தேர்வு செய்க--", "தாமரை", "நீல மாணிக்கம்", "நீலோற்பலம்", "மல்லிகை"])
q5 = st.selectbox("5. மனித உடலில் உள்ள எலும்புகள் எத்தனை?", ["--தேர்வு செய்க--", "200", "206", "210", "195"])
q6 = st.selectbox("6. ஒளிச்சேர்க்கைக்குத் தேவையான முக்கிய வாயு?", ["--தேர்வு செய்க--", "ஆக்ஸிஜன்", "நைட்ரஜன்", "கார்பன் டை ஆக்சைடு", "ஹைட்ரஜன்"])

# முன்னேற்றப் பட்டை
prog = 0
if q4 != "--தேர்வு செய்க--": prog += 33
if q5 != "--தேர்வு செய்க--": prog += 33
if q6 != "--தேர்வு செய்க--": prog += 34
st.progress(prog)

if st.button("விளையாட்டு 2 சரிபார்"):
    s2 = 0
    if q4 == "நீலோற்பலம்": s2 += 1
    if q5 == "206": s2 += 1
    if q6 == "கார்பன் டை ஆக்சைடு": s2 += 1
    if s2 == 3:
        st.snow()
        st.success("அற்புதம்! முழு மதிப்பெண்கள்!")
    else:
        st.warning(f"விளையாட்டு 2 மதிப்பெண்: {s2}/3")

st.title("🎡 வினாக்கள் அடங்கிய வண்ணச் சக்கரம்")

# கேள்விகளின் பட்டியல்
options = ["5 + 3", "10 + 2", "15 + 5", "7 + 4", "12 + 6", "9 + 9", "20 + 10", "8 + 6"]

if st.button("🎡 சக்கரத்தைச் சுழற்று!"):
    placeholder = st.empty()
    
    # 1. சக்கரம் சுழலும் உணர்வைத் தரும் அனிமேஷன் (15 முறை மாறும்)
    for i in range(15):
        pick = random.choice(options)
        # வெவ்வேறு நிறங்கள் மாறி மாறி வரும்
        colors = ["#FF4B4B", "#1C83E1", "#FFD700", "#7D3Cff", "#00C0F2"]
        bg_color = random.choice(colors)
        
        placeholder.markdown(f"""
            <div style="display: flex; justify-content: center; align-items: center; 
                        background-color: {bg_color}; color: white; border-radius: 50%; 
                        width: 250px; height: 250px; border: 10px solid white;
                        font-size: 35px; font-weight: bold; margin: auto;
                        box-shadow: 0px 0px 15px rgba(0,0,0,0.3); transform: rotate({i*24}deg);">
                {pick}
            </div>
        """, unsafe_allow_html=True)
        time.sleep(0.1) # சுழலும் வேகம்
    
    # 2. கடைசியாக ஒரு கேள்வியில் சக்கரம் நின்று பச்சை நிறமாக மாறும்
    final_q = random.choice(options)
    placeholder.markdown(f"""
        <div style="display: flex; justify-content: center; align-items: center; 
                    background-color: #2E7D32; color: white; border-radius: 50%; 
                    width: 250px; height: 250px; border: 10px solid gold;
                    font-size: 40px; font-weight: bold; margin: auto;
                    box-shadow: 0px 0px 25px gold;">
            {final_q}
        </div>
    """, unsafe_allow_html=True)
    st.session_state['current_q'] = final_q

# 3. பதில் சொல்லும் பகுதி
if 'current_q' in st.session_state:
    st.write(f"### 🎯 உங்களுக்கான வினா: {st.session_state['current_q']}")
    user_ans = st.text_input("பதிலை இங்கே எழுதவும்:")
    if st.button("சரிபார்"):
        correct_ans = answers.get(st.session_state['current_q'])
        if user_ans == correct_ans:
            st.balloons()
            st.success("வெற்றி! சரியான விடை!")
        else:
            st.error(f"தவறு! சரியான விடை: {correct_ans}")
